# API Keys
# Get from https://console.apify.com/account/integrations
APIFY_API_TOKEN=your_apify_token_here

# Database
DATABASE_PATH=data/jobs.db

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/job_monitor.log

# Scheduler
RUN_INTERVAL_MINUTES=30

# Agent workflow (Phase 2)
# Add whichever key your agent stack uses.
GOOGLE_API_KEY=your_google_api_key_here
OPENAI_API_KEY=your_openai_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here

# Root apply-decider uses a fixed OpenAI model configured in code:
# src/agents/root_apply_decider/agent.py

# How many NEW jobs to pull from DB per run
AGENT_BATCH_SIZE=100
AGENT_POLL_INTERVAL_SECONDS=60
AGENT_MAX_RETRIES=3
AGENT_RETRY_BACKOFF_SECONDS=300
AGENT_RETRY_BACKOFF_MULTIPLIER=3
# Auto-filter jobs posted more than this many days ago (0 = disabled, default 90)
JOB_MAX_AGE_DAYS=90

# Notifications (optional: used for terminal gate failures)
NTFY_TOPIC=
NTFY_SERVER=https://ntfy.sh
NTFY_TOKEN=
NTFY_PRIORITY=default

# Optional profile/config overrides
CANDIDATE_PROFILE_PATH=config/candidate_profile.yaml
SQLITE_JOURNAL_MODE=WAL

# Optional resume-tailor runtime command override.
# Example wrapper command:
# PI_CODING_AGENT_COMMAND=pnpm --dir /abs/path/to/pi-mono/packages/coding-agent <non-interactive-run-subcommand>
PI_CODING_AGENT_COMMAND=

# Model for the resume tailor pi agent.
# Recommended: openai/openai/gpt-5.1-mini-codex (latest GPT mini as of March 2026)
# Leave blank to use pi's built-in default.
RESUME_TAILOR_MODEL=

# Resume tailor worker (scripts/process_qualified_jobs.py)
TAILOR_POLL_INTERVAL_SECONDS=30
TAILOR_MAX_RETRIES=2
TAILOR_RETRY_BACKOFF_SECONDS=600
TAILOR_RETRY_BACKOFF_MULTIPLIER=2
TAILOR_CLAIM_LEASE_SECONDS=7200
TAILOR_OUTPUT_DIR=data/tailored_resumes
